import React from 'react';
import './ScrollingQuote.css';

const ScrollingQuote = () => {
    return (
        <div className="scrolling-container">
            <p className="scrolling-text">
                "Being full-stack developer provides end-to-end web development, handling both front-end (user interfaces) and back-end (server-side logic) aspects to create functional and interactive applications."
            </p>
        </div>
    );
};

export default ScrollingQuote;
